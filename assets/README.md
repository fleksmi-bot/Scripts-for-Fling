# Assets

- `mobile-performance-avatar.svg` is a generated text-based neon avatar icon for the mobile performance overlay.
- The avatar is stored as SVG instead of a binary PNG to avoid binary-data errors in patch/PR review tools.
